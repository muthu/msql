#include <iostream>
#include "utils.h"

/*
 * This function prints the prompt that the user is presented with
 */
void printPrompt(std::ostream& out) {
    out << "db > ";
}

/*
 * This function prints the prompt that the user is presented with
 */
void printPrompt() {
    std::cout << "db > ";
}
